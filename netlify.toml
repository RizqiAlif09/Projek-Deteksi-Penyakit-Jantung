[build]
  environment = { PYTHON_VERSION = "3.12.7" }
  command = "pip install -r requirements.txt && gunicorn -b 0.0.0.0:$PORT app:app"
  publish = "public"  # Sesuaikan jika Anda memiliki folder statis


[[redirects]]
  from = "/*"
  to = "/app.py"
  status = 200
